# SysDraw Web App

### Todos

- add an alert on first visit on mobile screens, prompting for using it on desktop
- add custom icons management when its added later on web layer (will need to make the nodes and groups in the canvas package pluggable entities, instead of hardcoding them)
- create diagram templates
- 