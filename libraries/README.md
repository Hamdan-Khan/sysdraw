# Libraries

ZeroSketch hosted icon libraries available in the canvas as built-in libraries.

## Todos

- replace the wikimedia icons with self hosted icons
- create a github worklfow to sync libraries from this repo to an object storage and serve as cdn.
- add guidelines for contributing to libraries
- add links to the official sites for external vendors' libraries
